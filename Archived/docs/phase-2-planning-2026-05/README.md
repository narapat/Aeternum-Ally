# Phase 2 Planning Archive

This directory preserves the Phase 2 planning package created in May 2026.

## Status

- The issue drafts map to GitHub issues `#27` through `#45`, which are closed.
- The `set*.zip` files duplicate groups of the loose Markdown drafts and are retained as original source artifacts.
- `aeternumally-phase2-skill.md` reflects the architecture and delivery assumptions at the time it was written. It is historical material, not current agent guidance.
- Current engineering guidance lives in the repository root `AGENTS.md`.

Do not use this archive as an implementation specification without comparing it with the current code, schema, security review, and GitHub issue state.
